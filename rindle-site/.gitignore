node_modules
dist

# Legacy static images bootstrapped from ../docs by `scripts/sync-assets.mjs` (predev/prebuild).
# Not committed here — re-synced on demand; they still live in the parent repo. `zero-knight.png`
# and any hand-added assets under public/ ARE committed.
/public/img
/public/blog-assets
# Built from ../packages/dha-report-app by the predev/prebuild lifecycle.
/public/dha-report-app
.rindled
*.db
*.db-*
.wrangler
.dev.vars
.dev.vars.*
!.dev.vars.example
.env
.env.*
!.env.example
.DS_Store
*.log

# Rindle Cloud: ignore the rendered manifests, but COMMIT the binding (.rindle/cloud.json) so
# `rindle deploy` re-attaches future deploys to the same managed app.
/rindle.json
/.rindle/*
!/.rindle/cloud.json
